# DEPRECATED / DECOMMISSIONED
# ===========================
# The environmental-poller is now run as a Docker container service
# managed by Docker Compose under the container name: environmental-poller.
#
# DO NOT enable or run this systemd service on the host.
#
# To manage the new poller, use Docker Compose on the production host:
#   docker compose up -d environmental-poller
#   docker compose logs -f environmental-poller
#   docker compose stop environmental-poller
